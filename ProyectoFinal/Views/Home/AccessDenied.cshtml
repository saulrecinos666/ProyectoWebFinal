@{
    ViewData["Title"] = "Acceso Denegado";
    Layout = "~/Views/Shared/_Layout.cshtml"; // Asegúrate de usar tu layout principal
}

<div class="container text-center py-5">
    <h1 class="display-4 text-danger mb-4">
        <i class="fas fa-exclamation-triangle fa-2x me-3"></i> Acceso Denegado
    </h1>
    <p class="lead mb-4">
        No tienes los permisos necesarios para acceder a esta página.
    </p>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="alert alert-info" role="alert">
                <h5 class="alert-heading">¿Qué puedes hacer?</h5>
                <ul class="list-unstyled">
                    <li><i class="fas fa-arrow-left me-2"></i> Puedes regresar a la <a href="@Url.Action("Index", "Home")" class="alert-link">página principal</a>.</li>
                    <li><i class="fas fa-sign-in-alt me-2"></i> Asegúrate de haber <a href="@Url.Action("Login", "Home")" class="alert-link">iniciado sesión</a> con la cuenta correcta.</li>
                    @if (!string.IsNullOrEmpty(Context.Request.Query["ReturnUrl"]))
                    {
                        var returnUrl = Context.Request.Query["ReturnUrl"];
                        <li><i class="fas fa-undo me-2"></i> Si crees que esto es un error y tienes los permisos, intenta ir a <a href="@(returnUrl)" class="alert-link">la página original</a> después de iniciar sesión correctamente.</li>
                    }
                    <li><i class="fas fa-user-shield me-2"></i> Contacta al administrador del sistema si crees que necesitas acceso a esta funcionalidad.</li>
                </ul>
            </div>
        </div>
    </div>

    @* Opcional: Mostrar el ReturnUrl para depuración (quitar en producción) *@
    @* <p class="mt-5 text-muted">ReturnUrl: @Context.Request.Query["ReturnUrl"]</p> *@
</div>